//! `WinUI` backend support for `WaterUI` CLI.
//!
//! This module provides `WinUI` platform support for building and running
//! `WaterUI` applications as native `WinUI` 3 applications on Windows.

pub mod backend;
pub mod device;
pub mod platform;
pub mod toolchain;
